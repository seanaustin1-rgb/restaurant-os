# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
# Claude Code: ignore local/personal config, but track the shared session hook,
# settings, and vendored skills (e.g. impeccable, symlinked into .agents/skills)
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/skills/
.claude/settings.local.json

# debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/migrations/dev.db*

# local dev artifacts holding real financial data — never commit
/scripts/.cache-txns.json
/scripts/*.cache.json
